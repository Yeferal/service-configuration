server:
  port: 8280
  servlet:
    context-path: /api/auth

spring:
  datasource:
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5450/erp_dev}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6380}
      password: ${REDIS_PASSWORD:passredis}

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}

cors:
  allowed-origins: "*"
  allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"

logging:
  level:
    root: INFO
    org:
      springframework:
        security: DEBUG
        web: DEBUG
    web: debug
    com.hw.mono_microservice: DEBUG