server:
  port: 8761

eureka:
  instance:
    hostname: eureka-microservice
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka-microservice:${server.port}/eureka/